{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npx tsc -p sync-bridge/tsconfig.json && npx tsc"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3"
  }
}
